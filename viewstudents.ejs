<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>View Registered Students (Admin)</title>

    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="admincss.css">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css" />
    <style>
        .table-container {
            margin-top: 40px;
        }
        h2 {
            text-align: center;
            margin-top: 20px;
            margin-bottom: 30px;
        }
        .table th, .table td {
            vertical-align: middle;
        }
        .table th {
            white-space: nowrap;
        }
        .verified-status {
            font-weight: bold;
        }
        .verified-yes {
            color: green;
        }
        .verified-no {
            color: orange;
        }
    </style>
</head>
<body>
    
    <!-- Include Admin Navigation -->
    <!-- <%- include('navforadmin.ejs') %> -->
      <%- include('studentsHome.ejs') %>

    <div class="container table-container">
        <h2>📋 Registered Students</h2>

        <% if (message && message.length > 0) { %>
            <div class="alert alert-success" role="alert">
                <%= message %>
            </div>
        <% } %>

        <% if (error && error.length > 0) { %>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
        <% } %>

        <% if (students && students.length > 0) { %>
            <div class="table-responsive">
                <table class="table table-bordered table-striped mt-4">
                    <thead class="thead-dark">
                        <tr>
                            <th>#</th>
                            <th>Student ID</th>
                            <th>Full Name</th>
                            <th>Email</th>
                            <th>Username</th>
                            <th>Contact</th>
                            <th>Email Verified</th>
                            <th>Contact Verified</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% students.forEach(function(student, index) { %>
                            <tr>
                                <td><%= index + 1 %></td>
                                <td><%= student.stid %></td>
                                <td><%= student.name %></td>
                                <td><%= student.email %></td>
                                <td><%= student.username %></td>
                                <td><%= student.contact %></td>
                                <td class="verified-status <%= student.is_email_verified ? 'verified-yes' : 'verified-no' %>">
                                    <%= student.is_email_verified ? 'Yes' : 'No' %>
                                </td>
                                <td class="verified-status <%= student.is_contact_verified ? 'verified-yes' : 'verified-no' %>">
                                    <%= student.is_contact_verified ? 'Yes' : 'No' %>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        <% } else { %>
            <div class="alert alert-info mt-4" role="alert">
                No students found.
            </div>
        <% } %>
    </div>

    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.14.3/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/js/bootstrap.min.js"></script>
</body>
</html>
